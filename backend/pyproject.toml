[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["routes", "auth", "database", "dependencies", "google_services", "models", "schemas"]
omit = ["tests/*", "venv/*", "alembic/*"]

[tool.coverage.report]
show_missing = true
fail_under = 80
